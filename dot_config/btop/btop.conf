# btop: catppuccin latte (theme vendored at themes/catppuccin_latte.theme).
# Set theme_background = False here if you want the terminal background to show
# through instead of btop painting its own.
color_theme = "catppuccin_latte"
